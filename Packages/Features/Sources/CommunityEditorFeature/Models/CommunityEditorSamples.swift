enum CommunityEditorSamples {
  static let blockSample = """
    # Встреча Friendly

    **Жирный**, *курсив*, ~~зачёркнутый~~ и [ссылка](https://example.com).

    > Собираемся в субботу.

    - Кофе
    - Прогулка

    ```swift
    let greeting = "Привет 👋"
    ```

    | День | Место | Участников |
    | :--- | :---: | ---: |
    | **Суббота** | Парк | 12 |
    | Воскресенье | Кофейня | 3 |
    | Понедельник | | 0 |
    """

  static let paragraphSample = """
    Давайте встретимся **в субботу** 👋
    Можно взять кофе и прогуляться.

    Это второй абзац: *курсив*, ~~отмена~~, `код` и [ссылка](https://example.com).

    Выдели часть текста и попробуй форматирование — переносы теперь не блокируют редактор.
    """

  static let listSample = """
    ## План встречи

    - [x] Выбрать место
    - [ ] Договориться о времени
      - Утром — кофе
      - Вечером — прогулка

    7. Встречаемся у входа
    8. Идём в парк

    > Можно прийти с друзьями.
    >
    > **Главное** — хорошее настроение.

    ---

    https://getfriend.ly
    """


  static let completeSample =
    blockSample + "\n\n" + listSample + "\n\n" + """
      ### Форматирование внутри абзаца

      Обычный текст, **жирный**, *курсив*, ***жирный курсив***,
      ~~зачёркнутый~~, `inline code` и **`жирный код`**.
      [Ссылка с **выделением**](https://example.com).

      > Цитата с **форматированием**.
      >
      > > Вложенная цитата с `кодом`.

      #### Изображение

      Текст перед изображением.

      ![Горы и озеро](https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?w=800&q=80)

      Текст после изображения. Загрузка требует интернета.

      ---

      #### HTML показываем как исходник

      <div>Без выполнения HTML</div>
      """

  static let short = "Привет, **Friendly**! Давайте знакомиться 👋"

}
